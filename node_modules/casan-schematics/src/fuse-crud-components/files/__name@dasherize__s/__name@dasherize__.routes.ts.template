import { Routes } from '@angular/router';
import { CanDeactivateUserEdit } from './<%= dasherize(name) %>.guard';
import { <%= dasherize(name) %>ListsResolver, <%= dasherize(name) %>Resolver } from './<%= dasherize(name) %>.resolver';
import { <%= classify(name) %>Component } from './<%= dasherize(name) %>.component';
import { Edit<%= classify(name) %>Component } from './edit/edit.component';
import { <%= classify(name) %>ListComponent } from './list/list.component';

export default [
    {
        path: '',
        component: <%= classify(name) %>Component,
        children: [
            {
                path: '',
                component: <%= classify(name) %>ListComponent,
                resolve: {
                    initialData: <%= dasherize(name) %>ListsResolver,
                },
                children: [
                    {
                        path: 'create',
                        component: Edit<%= classify(name) %>Component,
                        canDeactivate: [CanDeactivateUserEdit],
                    },
                    {
                        path: 'edit/:id',
                        component: Edit<%= classify(name) %>Component,
                        resolve: {
                            initialData: <%= dasherize(name) %>Resolver,
                        },
                        canDeactivate: [CanDeactivateUserEdit],
                    },
                ],
            },
        ],
    },
] as Routes;
