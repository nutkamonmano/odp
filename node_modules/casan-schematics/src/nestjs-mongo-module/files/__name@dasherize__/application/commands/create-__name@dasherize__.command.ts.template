import { RequestUserDto } from 'src/common/presentation/dtos/request-user.dto';
import { Create<%= classify(name) %>Dto } from '../dtos/create-<%= dasherize(name) %>.dto';

export class Create<%= classify(name) %>Command {
  constructor(
    public readonly create<%= classify(name) %>Dto: Create<%= classify(name) %>Dto,
    public readonly createdBy: RequestUserDto,
  ) {}
}
