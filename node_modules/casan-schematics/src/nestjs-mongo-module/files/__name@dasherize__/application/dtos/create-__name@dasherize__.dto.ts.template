import { ApiProperty } from '@nestjs/swagger';
import { IsString, IsEmail, IsArray } from 'class-validator';

export class Create<%= classify(name) %>Dto {
  @ApiProperty({ description: 'Name of the <%= dasherize(name) %>' })
  @IsString()
  name: string;

  companyId: string;
}