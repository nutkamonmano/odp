import { <%= classify(name) %>Entity } from '../entities/<%= dasherize(name) %>.entity';

export interface <%= classify(name) %>RepositoryInterface {
  // บันทึกข้อมูลใหม่
  save(<%= dasherize(name) %>: <%= classify(name) %>Entity): Promise<<%= classify(name) %>Entity>;

  // ค้นหาโดยใช้ ID
  findById(id: string): Promise<<%= classify(name) %>Entity | null>;

  // ค้นหาโดยใช้ Name
  findByName(name: string): Promise<<%= classify(name) %>Entity | null>;

  // ดึงข้อมูลทั้งหมด (แบบแบ่งหน้า)
  findAllPaginated(
    page: number,
    limit: number,
    sortBy: string,
    sortType: string,
    keyword: string,
    companyId: string,
  ): Promise<{ data: <%= classify(name) %>Entity[]; totalCount: number }>

  // นับจำนวนทั้งหมด
  count(): Promise<number>;

  // อัปเดตข้อมูล
  update(<%= dasherize(name) %>: <%= classify(name) %>Entity): Promise<<%= classify(name) %>Entity>;

  // ลบตาม ID
  delete(id: string): Promise<void>;
}
