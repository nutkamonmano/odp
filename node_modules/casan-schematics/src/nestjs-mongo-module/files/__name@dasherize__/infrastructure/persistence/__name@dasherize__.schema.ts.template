import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
import { Expose } from 'class-transformer';
import { <%= classify(name) %>Entity } from 'src/<%= dasherize(name) %>/domain/entities/<%= dasherize(name) %>.entity';

@Schema()
export class <%= classify(name) %> extends <%= classify(name) %>Entity {}

export const <%= classify(name) %>Schema = SchemaFactory.createForClass(<%= classify(name) %>);
